{
  "private": true,
  "main": "index.js",
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "build": "turbo run build --filter=forge-inspector",
    "env": "env",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint --filter=forge-inspector",
    "prerelease": "pnpm run build",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.23.0",
    "eslint": "8.57.1",
    "turbo": "^2.3.0"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "18.3.12",
      "@types/node": "22.8.6"
    }
  }
}